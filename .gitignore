# macOS
.DS_Store

# Windows
Thumbs.db

# Node / build artifacts (safe to include even if unused)
node_modules/
dist/
build/

# wrangler dev / vitest-pool-workers local state — machine-specific, never committed
.wrangler/

# Editor settings
.vscode/

# Claude Code agent worktrees — transient, machine-specific
.claude/worktrees/